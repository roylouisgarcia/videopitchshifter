# Video Pitch Adjuster Requirements
# External system requirement: FFMPEG must be installed and on your PATH
# Verify with: ffmpeg -version

# Desktop GUI (video_pitch_adjuster.py) uses only built-in Python libraries:
# tkinter, subprocess, threading, tempfile, pathlib, os

# Web app dependencies are listed in webapp/requirements.txt
